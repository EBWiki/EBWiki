name: Hanami

on: [pull_request, workflow_dispatch]

# CI uses an ephemeral local Postgres with trust auth — no password in DATABASE_URL.
# Session and other secrets come from Doppler. Do not inject the live Rails
# DATABASE_URL into this job (Railway ebwiki-web / Neon ebwiki-review).

jobs:
  rspec:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/hanami

    env:
      HANAMI_ENV: test
      DATABASE_URL: postgres://postgres@localhost:5432/ebwiki_hanami_test
      BUNDLE_IGNORE_FUNDING_REQUESTS: 1

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v3.7.0

      - name: Fetch secrets from Doppler
        if: ${{ secrets.DOPPLER_TOKEN != '' }}
        id: doppler
        uses: dopplerhq/secrets-fetch-action@v2.0.0
        with:
          doppler-token: ${{ secrets.DOPPLER_TOKEN }}

      - name: Guard local CI database
        env:
          SESSION_SECRET: ${{ steps.doppler.outputs.SESSION_SECRET || secrets.SESSION_SECRET }}
        run: |
          case "$DATABASE_URL" in
            postgres://postgres@localhost:5432/ebwiki_hanami_test|postgres://postgres@127.0.0.1:5432/ebwiki_hanami_test)
              ;;
            *)
              echo "Refusing Hanami CI against a non-local DATABASE_URL" >&2
              exit 1
              ;;
          esac

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4.2
          bundler-cache: true
          working-directory: apps/hanami

      - name: Create database
        env:
          SESSION_SECRET: ${{ steps.doppler.outputs.SESSION_SECRET || secrets.SESSION_SECRET }}
        run: |
          psql -h localhost -U postgres -c "CREATE DATABASE ebwiki_hanami_test;"
          bundle exec rake db:load_schema

      - name: Install Node packages
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/hanami/package-lock.json

      - name: Compile assets
        env:
          SESSION_SECRET: ${{ steps.doppler.outputs.SESSION_SECRET || secrets.SESSION_SECRET }}
        run: |
          npm ci
          bundle exec hanami assets compile

      - name: Run RSpec
        env:
          SESSION_SECRET: ${{ steps.doppler.outputs.SESSION_SECRET || secrets.SESSION_SECRET }}
        run: bundle exec rspec

  rubocop:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/hanami

    env:
      BUNDLE_IGNORE_FUNDING_REQUESTS: 1

    steps:
      - name: Checkout code
        uses: actions/checkout@v3.7.0

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4.2
          bundler-cache: true
          working-directory: apps/hanami

      - name: Run Standard Ruby
        run: bundle exec standardrb

  dawnscanner:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/hanami

    env:
      BUNDLE_IGNORE_FUNDING_REQUESTS: 1

    steps:
      - name: Checkout code
        uses: actions/checkout@v3.7.0

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4.2
          bundler-cache: true
          working-directory: apps/hanami

      - name: Run bundler-audit
        run: bundle exec bundler-audit check --update

      - name: Install Dawnscanner knowledge base
        run: bundle exec rake security:dawn_kb

      - name: Run Dawnscanner
        run: bundle exec dawn scan .
