

<div class="">
  <ul class="col-md-12">
    <% @comments.each do |comment| %>
     <!--  <a class="pull-left" href="#">
          <img class="media-object" src="http://placehold.it/64x64" alt="">
      </a> -->
      <li class="col-md-12">
        <small style="color: green;"><%= time_ago_in_words(comment.created_at) %> ago <%= link_to comment.user.name, user_path(comment.user) %></small>
        <p><%= simple_format comment.content %></p>
      </li>
    <% end %>
  </ul>
</div>