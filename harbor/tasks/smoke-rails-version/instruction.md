# Print the EBWiki Rails version

This environment is a checkout of EBWiki at `/usr/src/ebwiki`.

Write the output of `bundle exec rails -v` to `/tmp/rails_version.txt`.

The file must exist and contain the word `Rails`.
