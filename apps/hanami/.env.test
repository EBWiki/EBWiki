# Used when HANAMI_ENV=test. Override locally with Doppler (`doppler run`)
# or `.env.test.local`. No password in this URL; CI Postgres uses trust auth.
DATABASE_URL=postgres://postgres@localhost:5432/ebwiki_hanami_test
SESSION_SECRET=test-session-secret-not-for-production-must-be-at-least-64-characters-long
