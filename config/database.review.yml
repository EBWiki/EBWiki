default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  # Neon PgBouncer pooler rejects cached plans after schema changes.
  prepared_statements: false

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
